import { Metadata } from "next";

export const metadata: Metadata = {
  title: "Thủ thuật game",
  description: "Thủ thuật game",
};

const UsersPage = async (props: any) => {
  return <div>Thủ thuật game</div>;
};

export default UsersPage;
