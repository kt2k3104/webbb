import { Metadata } from "next";

export const metadata: Metadata = {
  title: "GiftCode",
  description: "GiftCode",
};

const UsersPage = async (props: any) => {
  return <div>GiftCode</div>;
};

export default UsersPage;
